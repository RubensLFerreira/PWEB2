const base = Number(prompt("Digite o valor em cm da base do retangulo: "));
const altura = Number(prompt("Digite o valor em cm da altura do retangulo: "));

const area = base * altura;

document.write(`A área do retagulo é de ${area} cm.`);
